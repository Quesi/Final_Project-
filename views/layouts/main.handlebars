<!doctype html>
<html>
<head>
    <title>Todo List</title>
    <link rel="stylesheet" href="/css/style.css"/>
</head>
<body>
  <header>
    <h1>Ghana Poetry!</h1>
    <nav>
      <ul>
        <li><a href="/">HOLES IN MY LACE PANTY</a></li>
        <li><a href="/poem1">THANK YOU</a></li>
        <li><a href="/poem2">Xray</a></li>
        <li><a href="/poem3">PROBABLY NUTS</a></li>
        <li><a href="/poem4">SWEET MEMORIES</a></li>
      </ul>
    </nav>
  </header>
  <section id="wrapper">
  	 {{{ body }}}
  </section>
  <footer>Ghana Poetry!</footer>



<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js" type="text/javascript"></script>
<script type="text/javascript">

    $(document).ready(function () {

        $(document).on("submit", "form", function (e) {
            e.preventDefault();
            return false;
        });

        // Initial loading of tasks
        for (var i = 0; i < localStorage.length; i++) {
            $("#comments").append("<ul id='comment-" + i + "'>" + localStorage.getItem('comment-' + i) +
                    "</text>");

            //<a href='#'>Delete</a>
        }

      //   Add a task
        $("form").submit(function (e) {
            e.preventDefault();
            e.stopImmediatePropagation();
            if ($("#comment").val() != "") {
                localStorage.setItem("comment-" + i, $("#comment").val());
                $("#comments").append("<li id='comment-" + i + "'>" + localStorage.getItem("comment-" + i) +
                        " </li>");
                $("#comment").val("");
                i++;
                //<a href='#'>Delete</a>
            }
            return false;
        });

        // Remove a task

//        $("#tasks li a").live("click", function () {
//            localStorage.removeItem($(this).parent().attr("id"));
//            $(this).parent().hide();
//            for (var i = 0; i < localStorage.length; i++) {
//                if (!localStorage.getItem("task-" + i)) {
//                    localStorage.setItem("task-" + i, localStorage.getItem('task-' + (i + 1)));
//                    localStorage.removeItem('task-' + (i + 1));
//                }
//            }
//        })

    });

</script>
</body>
</html>
